# Aplication of Minimum Spanning Tree Prim's Algorithm

### This program gives the optimal way of interconnect cities with optical fiber.

### How to run: 
`$ pip3 install -r requirements.txt` 
`$ python3 main.py` 
